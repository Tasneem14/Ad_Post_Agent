{
  "media_model_input": "string",

  "media_type": "image | video | photo_carousel",

  "media_constraints": {

    "allowed_types": ["image", "video", "photo_carousel"],
    "selected_type": "image | video | photo_carousel",

    "image": {
      "aspect_ratio": "string",
      "min_resolution": "string",
      "max_file_size_mb": "number",
      "text_overlay_allowed": "boolean",
      "branding_required": "boolean (optional)",
      "branding_position": "string (optional)"
    },

    "video": {
      "max_duration_sec": "number",
      "aspect_ratio": "string (optional)",
      "aspect_ratios": ["string"] ,
      "captions_required": "boolean",
      "hook_first_sec": "number (optional)",
      "branding_first_sec": "number (optional)"
    },

    "image_count": {
      "min": "number",
      "max": "number"
    },

    "aspect_ratio": "string (for carousel)",
    "safe_zone_required": "boolean",
    "ugc_style": "boolean",
    "supports_audio": "boolean",
    "recommended_use_cases": ["string"]
  },

  "reference_image": "optional file path or binary",
  "video_init_image": "optional file path or binary"
}
